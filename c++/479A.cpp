#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <algorithm>
using namespace std;
/*int a[5];
int main()
{
	int num[5] = {0};
	for(int i = 0; i < 3; i++)
	scanf("%d", &a[i]);
	num[0] = a[0] + (a[1]* a[2]);
	num[1] = a[0]*(a[1]+a[2]);
	num[2] = a[0]*a[1]*a[2];
	num[3] = (a[0]+a[1])* a[2];
	num[4] = a[0]+a[1]+a[2];
	sort(num, num+5);
	printf("%d\n", num[4]);
	return 0;
}*/

