#include <iostream>
#include <cstdio>
#include <cstring>
using namespace std;
int a[100005];
int find(int k)
{
	
	
	
	
}
int main()
{
	int n, x;
	while(scanf("%d%d", &n, &x) != EOF)
	{
		for(int i = 0; i < n; i++)
		{
			scanf("%d", &a[i]);
		}
		
	}
	return 0;
}
