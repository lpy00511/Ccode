#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;
int p[500005], m[500005]={0};
char str[500005];
int main()
{
	int n, ans, max_n;
	scanf("%d", &n);
	for(int i = 0; i < n; i ++)
	{
		scanf("%d", &p[i]);
	}
	scanf("%s", str);
	ans = 0, max_n;
	for(int i = 0; i < n; i++)
	{
		for(int j = i; j < n; j++)
		{
			if()
		}
	}
	return 0;
}
