#include <iostream>
#include <cstdio>
#include <cstdlib>
using namespace std;
int main()
{
	int n,k;
	int a[100005];
	scanf("%d%d", &n, &k);
	for(int i = 0; i < k; i ++)
	{
		scanf("%d", &v);
		for(int j = 0; j < v; j ++)
		{
			scanf("%d", &a[j]);
			if(a[i] != 1)
			break;
			else 
			{
				
	}
	return 0;
}
